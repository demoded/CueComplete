# Implementation Plan - Lock Bottom Cue Data Section to 20 Lines Height and Make Top Section Flexible

## Goal
Update the UI layout in [`UI/MainWindow.cs`](file:///D:/git/CueComplete/UI/MainWindow.cs) so that:
1. The bottom section (`Source cue details` and `Found cue data`) is locked to 20 lines in height.
2. The middle section (`_sourcePathLabel`) remains 1 line in height.
3. The top section (`Cue Files` and `Search Results`) is flexible and expands/shrinks to fill all remaining vertical space.

## Layout Analysis & Design
- **Top row (`_cueFilesPane` and `_searchResultsPane`)**:
  - `Height = Dim.Fill(21)` (allocating 1 line for source path label + 20 lines for bottom details frames).
  - `Width`: `Dim.Percent(20)` for Cue Files, `Dim.Fill()` for Search Results.
- **Middle row (`_sourcePathLabel`)**:
  - `Y = Pos.Bottom(_cueFilesPane)`
  - `Height = 1`
  - `Width = Dim.Fill()`
- **Bottom row (`_sourceCueDetailsPane` and `_foundCueDataPane`)**:
  - `Y = Pos.Bottom(_sourcePathLabel)`
  - `Height = 20`
  - `Width`: `Dim.Percent(50)` for Source cue details, `Dim.Fill()` for Found cue data.

## Verification
1. Run layout tests in [`CueComplete.Tests/MainWindowTests.cs`](file:///D:/git/CueComplete/CueComplete.Tests/MainWindowTests.cs) ensuring view frames and layout hierarchy remain intact under varying window dimensions.
2. Run `dotnet test` across the full test suite.
3. Build and package release executable: `dotnet publish -c Release -r win-x64 -p:PublishSingleFile=true --self-contained true`.
4. Log progress in [`.agents/progress.md`](file:///D:/git/CueComplete/.agents/progress.md).
