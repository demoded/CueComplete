# Implementation Plan - Update UI Layout to Match 2026-09-19 Specification

## Overview
Update [`UI/MainWindow.cs`](file:///D:/git/CueComplete/UI/MainWindow.cs) to match the new UI layout defined in [`.agents/new layout 20260919.md`](file:///D:/git/CueComplete/.agents/new%20layout%2020260919.md) and clarified with user feedback:
1. **Top Row (Height = 9 lines)**:
   - **Cue Files pane** (Width = 20%): Title formatted as `Cue Files [01/99]` (zero-padded matching total digits, forward slash separator). Contains file list.
   - **Search Results pane** (Width = 80% / Dim.Fill()): Title `Search Results`. Contains search results list.
2. **Middle Row (Height = 1 line)**:
   - **Full source cue file path**: Single-line text label spanning full width without borders, showing raw path of the current CUE file.
3. **Bottom Row (Height = Dim.Fill())**:
   - **Source cue details pane** (Width = 50%): Displays metadata from the loaded/parsed source CUE file.
   - **Found cue data pane** (Width = 50% / Dim.Fill()): Displays metadata for the highlighted/selected item in the Search Results list.
4. **Status Bar**:
   - Add `~Enter~ Apply` hint to the status bar alongside `~^Q~ Quit`, `~S~ Deep Search`, and `~^L~ Toggle Log`.
5. **Tests & Packaging**:
   - Update tests in [`CueComplete.Tests/MainWindowTests.cs`](file:///D:/git/CueComplete/CueComplete.Tests/MainWindowTests.cs) to verify the updated counter formatting (`Cue Files [1/3]`, `Cue Files [01/99]`, `Cue Files [0/0]`), pane titles, and path updates.
   - Execute `dotnet test`, `dotnet build`, and `dotnet publish -c Release -r win-x64 -p:PublishSingleFile=true --self-contained true`.
